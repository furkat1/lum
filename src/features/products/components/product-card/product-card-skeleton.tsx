import { Skeleton } from "@mui/material";
import React from "react";

export const ProductCardSkeleton = () => {
  return <Skeleton variant="rounded" width={325} height={220} sx={{ borderRadius: "5.45px" }} />;
};
